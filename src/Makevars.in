PKG_CFLAGS = -I. -Ipcre -DHAVE_CONFIG_H -DPHAST_VERSION='@PHAST_VERSION@' -DRPHAST @LAPACK_FLAG@ 
PKG_LIBS = @LAPACK_LIBS@
SRCS = $(wildcard *.c)
PCRE_SRCS = $(wildcard pcre/*.c)
OBJECTS = $(SRCS:.c=.o) $(PCRE_SRCS:.c=.o)

all: $(SHLIB)
$(SHLIB): $(OBJECTS)
